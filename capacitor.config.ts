import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anosdigital',
  appName: 'ANOS DIGTTAL,
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
